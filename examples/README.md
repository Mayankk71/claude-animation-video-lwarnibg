Generated scripts and Veo3 prompt sets from `/generate-script` land here, one file per
scenario. Nothing is pre-generated — each file only exists once you've run
`/analyze-reel` on the reference reels, `/build-style-bible` to synthesize the Style
Bible, and `/generate-script "<scenario>"` for a specific objection.
