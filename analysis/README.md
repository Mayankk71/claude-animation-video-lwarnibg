One subfolder per analyzed reel, created by `/analyze-reel`: 9 specialist JSON files plus
`merged.json`/`merged.md` (the readable summary). Populated once you run the pipeline on
your reference videos — empty until then.
